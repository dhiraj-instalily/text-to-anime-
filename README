# AI Image Generator

Generates images based on text prompts using AI. It combines a React frontend with a Flask backend to interact with an image generation API.

## Features

- Image generation based on text prompts
- Negative prompts to refine image generation
- Display of generated images

## Setup
1. Run the application:
   ```
   python app.py
   ```
2. Open `http://127.0.0.1:5000` in your browser

## Technologies Used

- Frontend: React
- Backend: Flask
- Image Generation: API (details in `call_model.py`)

Open source: https://github.com/dhiraj-instalily/Animagine-xl-3.1